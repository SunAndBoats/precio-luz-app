{
  "name": "precio-luz-backend",
  "version": "0.1.0",
  "private": true,
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon --watch src --exec node src/server.js",
    "test": "jest --runInBand",
    "prisma:generate": "prisma generate"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.6.7",
  "prisma": "^5.13.0",
  "@prisma/client": "^5.13.0",
  "pg": "^8.11.2",
    "node-cron": "^3.0.2",
    "redis": "^4.6.7"
  },
  "devDependencies": {
    "jest": "^29.6.1",
    "supertest": "^6.4.2",
    "nodemon": "^2.0.22"
  }
}
