Preview branch notes

- Front reads prices from your backend using VITE_API_URL.
- Ensure VITE_API_URL is set in Vercel (Preview and Production).
- Local override: create .env.local with VITE_API_URL=http://localhost:4000 (or your API).
